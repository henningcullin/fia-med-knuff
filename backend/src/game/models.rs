pub struct Player {
    pub color: Color,
}

pub struct Piece {
    pub position: u32,
}

pub enum Color {
    Red,
    Orange,
    Yellow,
    Green,
    Blue,
    Purple,
    Pink,
}

pub struct Board {}
